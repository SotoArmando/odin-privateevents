<nav class="navbar navbar-expand-lg " id='nav'>
  <a class="navbar-brand" href="<%= root_path %>">Odin Private Events</a>


  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <a class="nav-link" href="<%= root_path %>">Home <span class="sr-only"></span></a>
      </li>
   
      <%if current_user.nil? %>
      <li class="nav-item">
        <a class="nav-link" href="<%= signup_path %>">SignUp</a>
      </li>
        <li class="nav-item">
          <a class="nav-link" href="<%= signin_path %>">SignIn</a>
        </li>
      <%else %>
        




    </ul>
    <form class="form-inline my-2 my-lg-0">
      <a class="nav-link"

         <% if current_user.nil? %>

        <%else %>
        href=  "<%= user_path(current_user) %>">

        <%= current_user.name %>
        <%end %>

        <span class="sr-only">(current)</span></a>
      <%= link_to 'Log out', sign_out_path, method: :delete , class: "btn btn-outline-danger my-2 my-sm-0" %>
      <%end %>
    </form>
  </div>
</nav>